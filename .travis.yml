matrix:
  include:
    - language: java
      install: true

      before_install:
        - cd Json2Excel

      script:
        - chmod +x ./gradlew check

    - language: node_js
      node_js:
        - "iojs"
        - "7"
      before_install:
        - cd test/test/ClientApp

      install: "npm install"
      script: "npm test"